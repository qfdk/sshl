import test from 'node:test';
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const [app, sidebar, list, settings] = await Promise.all(['ui/src/App.tsx', 'ui/src/layout/Sidebar.tsx', 'ui/src/features/connections/ConnectionList.tsx', 'ui/src/layout/SettingsDialog.tsx'].map(file => readFile(new URL(`../${file}`, import.meta.url), 'utf8')));
test('sidebar renders named groups and leaves ungrouped connections at the top level', () => { assert.match(list, /getUngroupedConnections/); assert.match(list, /data-connection-default-items/); assert.match(list, /groupConnections/); assert.match(sidebar, /<Button[\s\S]*id="new-connection-btn"/); assert.match(sidebar, /<Input[\s\S]*id="connection-search"/); assert.match(sidebar, /<Button[\s\S]*id="toggle-sidebar"[\s\S]*variant="ghost"/); assert.match(sidebar, /<Button[\s\S]*id="open-settings-btn"[\s\S]*variant="ghost"/); assert.match(app, /<Tabs[\s\S]*value=\{activeTabId\}[\s\S]*onValueChange=\{setActiveTabId\}/); assert.match(app, /TabsContent[\s\S]*value="terminal"[\s\S]*forceMount[\s\S]*data-\[state=inactive\]:hidden/); assert.match(app, /TabsContent[\s\S]*value="file-manager"[\s\S]*forceMount[\s\S]*data-\[state=inactive\]:hidden/); });
test('group sorting remains in the React settings dialog', () => { assert.match(settings, /setDraggedGroup/); assert.match(settings, /onPointerCancel/); assert.match(settings, /group-manager-groups/); });
