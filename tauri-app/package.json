{
  "name": "sshl-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:renderer": "node build-renderer.cjs",
    "build": "pnpm run build:renderer && pnpm tauri build",
    "dev": "pnpm run build:renderer && pnpm tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "ejs": "^5.0.2"
  }
}
