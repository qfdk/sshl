{
  "name": "sshl",
  "version": "1.0.0",
  "description": "Fast SSH client with file transfer capabilities",
  "main": "main.js",
  "author": "qfdk",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "build:renderer": "vite build && node build-renderer.js",
    "build:mac": "pnpm run build:renderer && NODE_ENV=production electron-builder --mac",
    "dev": "electron ."
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "electron-store": "^8.1.0",
    "ssh2": "^1.14.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.7.0"
  },
  "devDependencies": {
    "electron": "^28.2.3",
    "electron-builder": "^24.6.4",
    "vite": "^5.0.0"
  },
  "build": {
    "appId": "me.qfdk.sshl",
    "productName": "SSHL",
    "directories": {
      "output": "release",
      "buildResources": "."
    },
    "files": [
      "**/*",
      "!**/*.ts",
      "!*.map",
      "!release",
      "dist/**/*"
    ],
    "mac": {
      "category": "public.app-category.utilities",
      "icon": "icon.png",
      "target": [
        "dir"
      ]
    }
  }
}
