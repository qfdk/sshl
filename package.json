{
  "name": "sshl",
  "version": "0.2.1",
  "description": "Fast SSH client with file transfer capabilities (Tauri)",
  "author": "qfdk",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "tauri": "tauri",
    "build:renderer": "node build-renderer.cjs",
    "build": "pnpm run build:renderer && pnpm tauri build --bundles app",
    "dev": "pnpm run build:renderer && pnpm tauri dev"
  },
  "dependencies": {
    "xterm": "^5.3.0",
    "xterm-addon-canvas": "^0.5.0",
    "xterm-addon-fit": "^0.7.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "ejs": "^5.0.2"
  }
}
