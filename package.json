{
  "name": "sshl",
  "version": "1.0.0",
  "description": "Fast SSH client with file transfer capabilities (Tauri)",
  "author": "qfdk",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "pnpm --dir tauri-app run build",
    "dev": "pnpm --dir tauri-app run dev"
  },
  "dependencies": {
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.7.0"
  }
}
